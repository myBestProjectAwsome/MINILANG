#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = (10 + 5);
    int y = (x * 2);
    int z = (y - 3);
    printf("%d\n", z);
    int a = (100 / 4);
    int b = (a + 10);
    printf("%d\n", b);
    return 0;
}